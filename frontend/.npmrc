# Keep npm's writable cache inside the repository. This avoids permissions and
# stale archive errors from a machine-wide Node.js cache on Windows.
cache=./.npm-cache-standalone
prefer-online=true
fund=false
audit=false
