# 验证记录

2026-09-09，本地 Windows、Python 3.12、Node.js 24 环境：

- scripts/check.ps1 完整通过。
- Ruff 静态检查与 41 个 Python 文件格式检查通过。
- pytest：14 项通过，覆盖 API、Hook、迁移、事务回滚和临时配置安装。
- Vitest：5 项通过，覆盖筛选分页、异步失败、过期响应和显示格式。
- Prettier 格式检查通过。
- TypeScript 检查与 Vite 生产构建通过。
- Playwright：1 项通过，使用 Edge 检查列表、详情、查询和标签切换，无页面异常。
- 从非项目工作目录启动真实 API 子进程，健康检查通过；使用临时数据库。
- pip check 无依赖冲突；git diff --check 通过。
- 检查 Git 文件清单，未发现数据库、日志、环境私密配置或依赖安装目录。

浏览器首次执行受到沙箱子进程清理权限限制，用例已通过；在允许正常清理的环境
重跑后，命令以 0 退出，耗时 11.9 秒。

现有提示：FastAPI/Starlette 测试客户端依赖出现弃用警告；Vite 提示第三方
PURE 注释及主包体积较大。均不导致检查失败。没有为消除提示擅自升级业务依赖。
GitHub Actions 工作流已添加但尚未在远程执行。
客户端真实 Hook 安装和私人历史数据未用于此次验证。
