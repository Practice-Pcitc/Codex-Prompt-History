[tool.pytest.ini_options]
testpaths = ["hook/tests", "backend/tests"]
addopts = "-q"
pythonpath = ["backend", "hook"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
